name: Django CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: django-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DJANGO_SETTINGS_MODULE: config.settings
      DJANGO_DEBUG: '0'
      DJANGO_SECRET_KEY: 'ci-only-secret-key-7f2d9a4c6e8b1f3a5d7c9e2f4a6b8c0d1e3f5a7b9c2d4e6f8a0b2c4d6e8f1a3'
      DJANGO_ALLOWED_HOSTS: 'localhost,127.0.0.1'
      DJANGO_CSRF_TRUSTED_ORIGINS: 'https://localhost'
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: python manage.py check --deploy
      - run: python manage.py migrate --run-syncdb --noinput
      - run: python manage.py seed_property_types
      - run: python manage.py test --noinput
