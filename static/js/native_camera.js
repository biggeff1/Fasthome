/* Fasthome — appareil photo géré exclusivement par photo_upload.js.
 *
 * Ce fichier est conservé pour compatibilité avec les anciens templates.
 * Ne rien injecter ici : photo_upload.js crée un champ camera séparé,
 * optimise immédiatement la photo puis l'ajoute au champ multipart principal.
 */
(() => {
  'use strict';
})();
